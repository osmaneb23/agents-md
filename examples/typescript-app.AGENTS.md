# AGENTS.md

Generated by `agents-md`. Keep manual notes outside managed sections so `agents-md update` can preserve them.

<!-- agents-md:start:stack -->
## Stack
- Framework: Next.js 15.3.0, React 19.0.0
- Language: TypeScript (strict mode)
- Package Manager: pnpm 10.0.0
- Runtime: Node.js
- Test Runner: vitest 3.0.0
<!-- agents-md:end:stack -->

<!-- agents-md:start:commands -->
## Commands
- run: `pnpm dev` from package.json. (next dev)
- build: `pnpm build` from package.json. (next build)
- lint: `pnpm lint` from package.json. (eslint .)
- test: `pnpm test` from package.json. (vitest run)
<!-- agents-md:end:commands -->

<!-- agents-md:start:testing -->
## Testing
- Full suite: `pnpm test`
- Single test: `pnpm vitest run <path> -t "<name>"`
- Prefer the narrowest relevant test while iterating, then run the broader check before release changes.
<!-- agents-md:end:testing -->

<!-- agents-md:start:conventions -->
## Conventions
- Package code uses a `src/` layout; import through the installed package path. (src/)
- TypeScript path aliases are configured in tsconfig: @/*. (tsconfig.json)
- JavaScript/TypeScript source uses named exports; no default exports were detected in scanned files. (src/api/client.ts, src/components/Button.tsx, src/components/index.ts)
- Barrel file detected at `src/components/index.ts`; prefer imports through `@/components` when using that module boundary. (src/components/index.ts)
- `Result<...>` return types appear in `src/api/client.ts`; handle those errors as values instead of assuming exceptions. (src/api/client.ts)
- HTTP calls appear centralized in `src/api/client.ts`; use that client/wrapper before adding direct fetch calls. (src/api/client.ts)
- Catch blocks in `src/api/client.ts` return fallback/error values; preserve that non-throwing error flow where it is used. (src/api/client.ts)
- Expected environment variables are documented in `.env.example`: API_BASE_URL, PUBLIC_SITE_URL. (.env.example)
<!-- agents-md:end:conventions -->

<!-- agents-md:start:boundaries -->
## Boundaries
### Always Do
- Run the narrowest relevant test before handing off a code change.
### Ask First
- Ask before deleting files, rewriting public history, changing release/package metadata, or running commands that mutate databases/infrastructure.
### Never Do
- Never commit secrets, tokens, or `.env` files.
- Never overwrite a hand-written AGENTS.md without `--force` or an explicit confirmation.
- Never modify `vendor/` unless the user explicitly asks.
<!-- agents-md:end:boundaries -->

<!-- agents-md:start:security -->
## Security
- Treat API keys as environment-only configuration; do not write them into AGENTS.md or generated logs.
- Prefer `--no-llm` in CI unless a provider key is intentionally configured for synthesis.
- Review generated files before publishing because AGENTS.md is meant to guide automated agents.
<!-- agents-md:end:security -->

<!-- agents-md:fingerprint {"files":{".env.example":"339311f4d9af54324682a1023b016e243306fda86954b4942341937f1a235f4a","package.json":"bea1da676aa1df4a85f808c34c14e81549ba854dad5d7c3083b49695ba63dea1","tsconfig.json":"ad87443f71377f7c972c4000af939ba81f2486d1a7e17a1664139cf3111edb74"},"version":1} -->
